# Agency Agents
Agent .md files installed from .vendor/agency-agents via scripts/install-agents.sh.
Run that script to populate this directory with the approved subset.
See docs/AGENTS.md for the full reference.

