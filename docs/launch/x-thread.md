# X thread — lancio (23-25 giugno, dopo Show HN)

6 tweet. GIF demo nel tweet 1. Link repo SOLO nell'ultimo (l'algoritmo penalizza i link esterni nei primi tweet).

**1 (hook):**
> My content pipeline was 10 tools and a spreadsheet.
> So I built a self-hosted AI content team that researches, writes, reviews — and learns from what works.
> Open source, MIT. Here's how it works 🧵
> [GIF demo]

**2 (research + scoring):**
> It pulls from RSS, search and YouTube in parallel, dedupes semantically, then scores every item on dimensions I configure: relevance, credibility, trend signal, voice alignment.
> No more "what should I post about?"

**3 (GOD mode):**
> Every draft goes through a four-agent editorial board:
> critic → structure. fact-checker → hallucinations. creative → flatness. synthesis → verdict.
> Each agent catches a different class of errors. None of them can publish.

**4 (feedback loop — il differenziatore):**
> The part I'm most proud of: social metrics flow BACK into the scoring weights.
> High-performing content reshapes what it researches next.
> It doesn't just write — it learns what works for my audience.

**5 (costi + controllo):**
> Runs on a $5/day LLM budget cap (configurable — the pipeline refuses calls beyond it).
> Self-hosted: your data, your keys, your infra.
> And nothing ships without my approval. The name is about the busywork, not the judgment.

**6 (CTA):**
> MIT licensed. Default docker compose is 2 containers.
> ⭐ https://github.com/davidesilver/ZeroHuman-Agency
> Questions welcome — I'll answer everything.

Nota: il cap reale è `DAILY_COST_CAP_USD=5.0` (al giorno, non al mese) — il tweet 5 è già corretto.
LinkedIn: stesso contenuto come post singolo lungo, tono build-in-public, stesso giorno.
